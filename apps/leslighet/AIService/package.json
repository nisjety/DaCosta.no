{
  "name": "ai-model-service",
  "version": "1.0.0",
  "description": "Python-based AI model service with FastAPI",
  "scripts": {
    "start": "python models/model_api_server.py",
    "start:docker": "docker-compose up -d",
    "build": "docker-compose build",
    "stop": "docker-compose down"
  }
}
